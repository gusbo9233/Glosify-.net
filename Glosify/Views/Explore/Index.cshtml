@model ExploreIndexViewModel
@{
    Layout = "~/Views/Shared/_AppLayout.cshtml";
    ViewData["Title"] = Text["Explore.Title"];
    ViewData["HideAssistantPanel"] = true;
}

@section Styles {
    <link rel="stylesheet" href="~/css/quiz-library.css" asp-append-version="true" />
}

<section class="select-quiz-page library-page explore-page">
    <header class="select-header library-hero explore-hero">
        <div class="select-heading">
            <p class="select-kicker">
                <span class="material-symbols-outlined" aria-hidden="true">travel_explore</span>
                @Text["Explore.Title"] <span class="select-kicker-separator">/</span> @QuizLanguageDisplay.Name(Model.CurrentLanguage)
            </p>
            <h1 class="select-title">@Text["Explore.Discover"]</h1>
            <p class="select-subtitle">
                @Text["Explore.Intro"]
                <a class="language-change-link" asp-controller="Languages" asp-action="Index">@Text["Quiz.ChangeLanguage"]</a>
            </p>
            <div class="library-overview" aria-label="@Text["Explore.Summary"]">
                <span class="overview-item">
                    <span class="material-symbols-outlined" aria-hidden="true">folder_shared</span>
                    <strong>@Model.Collections.Count</strong> @(Model.Collections.Count == 1 ? Text["Quiz.CollectionOne"] : Text["Quiz.CollectionMany"])
                </span>
                <span class="overview-item">
                    <span class="material-symbols-outlined" aria-hidden="true">public</span>
                    <strong>@Model.Quizzes.Count</strong> @(Model.Quizzes.Count == 1 ? Text["Quiz.QuizOne"] : Text["Quiz.QuizMany"])
                </span>
            </div>
        </div>
        <div class="explore-hero-feature" aria-hidden="true">
            <span class="material-symbols-outlined">diversity_3</span>
            <div>
                <span>@Text["Explore.CommunityLibrary"]</span>
                <strong>@QuizLanguageDisplay.Name(Model.CurrentLanguage)</strong>
            </div>
        </div>
    </header>

    @if (TempData[NotificationKeys.Explore] is string exploreMessage)
    {
        <div class="page-message">@exploreMessage</div>
    }

    @if (Model.Collections.Any())
    {
        <section class="library-section library-section--collections" aria-labelledby="publicCollectionsHeading">
            <div class="library-section-header">
                <div class="library-section-copy">
                    <h2 id="publicCollectionsHeading">@Text["Explore.SharedCollections"] <span class="section-count">@Model.Collections.Count</span></h2>
                    <p>@Text["Explore.CollectionsHelp"]</p>
                </div>
            </div>
            <div class="quiz-grid">
                @foreach (var item in Model.Collections)
                {
                    <article class="quiz-card collection-card">
                        <a class="quiz-card-link" asp-controller="Explore" asp-action="Collection" asp-route-id="@item.Collection.Id">
                            <div class="quiz-card-top">
                                <div class="quiz-icon collection-icon"><span class="material-symbols-outlined">folder_shared</span></div>
                                <span class="quiz-card-kind">@Text["Explore.SharedCollection"]</span>
                                <span class="material-symbols-outlined quiz-arrow" aria-hidden="true">arrow_outward</span>
                            </div>
                            <h2 class="quiz-name">@item.Collection.Name</h2>
                            <p class="quiz-languages">@QuizLanguageDisplay.Name(item.Collection.Language)</p>
                        </a>
                        <div class="quiz-meta">
                            <span class="quiz-date"><span class="material-symbols-outlined" aria-hidden="true">calendar_today</span>@item.Collection.CreatedAt.ToString("MMM d, yyyy")</span>
                            <div class="quiz-meta-actions">
                                <span class="status-pill">@item.QuizCount @(item.QuizCount == 1 ? Text["Quiz.QuizOne"] : Text["Quiz.QuizMany"]) &middot; @Text["Quiz.NestedCount", item.CollectionCount]</span>
                                <form class="share-form" method="post" asp-controller="Explore" asp-action="CopyCollection" asp-route-id="@item.Collection.Id">
                                    @Html.AntiForgeryToken()
                                    <button class="btn-copy-public" type="submit" aria-label="@Text["Explore.CopyNamed", item.Collection.Name]">
                                        <span class="material-symbols-outlined" aria-hidden="true">library_add</span>
                                        @Text["Common.Copy"]
                                    </button>
                                </form>
                            </div>
                        </div>
                    </article>
                }
            </div>
        </section>
    }

    @if (Model.Quizzes.Any())
    {
        <section class="library-section library-section--quizzes" aria-labelledby="publicQuizzesHeading">
            <div class="library-section-header">
                <div class="library-section-copy">
                    <h2 id="publicQuizzesHeading">@Text["Explore.CommunityQuizzes"] <span class="section-count">@Model.Quizzes.Count</span></h2>
                    <p>@Text["Explore.QuizzesHelp"]</p>
                </div>
            </div>
            <div class="quiz-grid">
                @foreach (var item in Model.Quizzes)
                {
                    <article class="quiz-card">
                        <a class="quiz-card-link" asp-controller="Explore" asp-action="Details" asp-route-id="@item.Quiz.Id">
                            <div class="quiz-card-top">
                                <div class="quiz-icon"><span class="material-symbols-outlined">public</span></div>
                                <span class="quiz-card-kind">@Text["Explore.CommunityQuiz"]</span>
                                <span class="material-symbols-outlined quiz-arrow" aria-hidden="true">arrow_outward</span>
                            </div>
                            <h2 class="quiz-name">@item.Quiz.Name</h2>
                            <p class="quiz-languages">@QuizLanguageDisplay.Name(item.Quiz.SourceLanguage) &rarr; @QuizLanguageDisplay.Name(item.Quiz.TargetLanguage)</p>
                        </a>
                        <div class="quiz-meta">
                            <span class="quiz-date"><span class="material-symbols-outlined" aria-hidden="true">calendar_today</span>@item.Quiz.CreatedAt.ToString("MMM d, yyyy")</span>
                            <div class="quiz-meta-actions">
                                <span class="status-pill"><span class="status-dot" aria-hidden="true"></span>@item.WordCount @(item.WordCount == 1 ? Text["Settings.WordLower"] : Text["Settings.WordsLower"])</span>
                                <form class="share-form" method="post" asp-controller="Explore" asp-action="CopyQuiz" asp-route-id="@item.Quiz.Id">
                                    @Html.AntiForgeryToken()
                                    <button class="btn-copy-public" type="submit" aria-label="@Text["Explore.CopyNamed", item.Quiz.Name]">
                                        <span class="material-symbols-outlined" aria-hidden="true">library_add</span>
                                        @Text["Common.Copy"]
                                    </button>
                                </form>
                            </div>
                        </div>
                    </article>
                }
            </div>
        </section>
    }

    @if (!Model.Collections.Any() && !Model.Quizzes.Any())
    {
        <div class="empty-state">
            <div class="empty-icon"><span class="material-symbols-outlined">explore</span></div>
            <h2 class="empty-title">@Text["Explore.NothingPublic", QuizLanguageDisplay.Name(Model.CurrentLanguage)]</h2>
            <p class="empty-copy">@Text["Explore.EmptyHelp"]</p>
        </div>
    }
</section>
