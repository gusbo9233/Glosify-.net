@{
    Layout = "~/Views/Shared/_GlosifyLayout.cshtml";
    ViewData["Title"] = Text["Auth.ForgotTitle"];
}

<main class="auth-shell">
    <div class="brand-stack">
        <div class="brand-orb">
            <span class="material-symbols-outlined">graphic_eq</span>
        </div>
        <h1>Glosify</h1>
        <p class="brand-tagline">@Text["Auth.BrandTagline"]</p>
    </div>

    <section class="glass-card is-centered">
        <span class="material-symbols-outlined auth-icon-hero">lock_reset</span>
        <header class="auth-card-header">
            <h2>@Text["Auth.ForgotHeading"]</h2>
            <p>@Text["Auth.ForgotUnavailable"]</p>
        </header>
        <a class="back-to-login" asp-controller="Account" asp-action="Login">
            <span class="material-symbols-outlined">arrow_back</span>
            @Text["Auth.BackToLogin"]
        </a>
    </section>
</main>
