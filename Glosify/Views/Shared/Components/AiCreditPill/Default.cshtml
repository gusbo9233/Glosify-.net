@model Glosify.ViewComponents.AiCreditPillModel
@if (Model.IsAdmin)
{
    <a class="credit-pill" asp-controller="Admin" asp-action="AiCredits" title="@Text["Payments.AiCredits"]">
        <span class="material-symbols-outlined" aria-hidden="true">bolt</span>
        <span>@Text["Payments.CreditCount", Model.AvailableCredits]</span>
    </a>
    <a class="icon-button" asp-controller="Admin" asp-action="AiCredits" aria-label="@Text["Payments.AiCreditAdmin"]" title="@Text["Payments.AiCreditAdmin"]">
        <span class="material-symbols-outlined">settings</span>
    </a>
}
else
{
    <span class="credit-pill" title="@Text["Payments.AiCredits"]">
        <span class="material-symbols-outlined" aria-hidden="true">bolt</span>
        <span>@Text["Payments.CreditCount", Model.AvailableCredits]</span>
    </span>
}
