@model FlashcardQuizViewModel
@{
    Layout = "~/Views/Shared/_AppLayout.cshtml";
    ViewData["Title"] = Text["Quiz.FlashcardsTitle"];
    ViewData["ActiveNav"] = "quizzes";

    var quizName = Model.SelectedQuiz?.Name ?? Text["Quiz.FlashcardsTitle"];
    var isFreestyle = Model.SelectedQuiz?.IsFreestyle == true;
}

<section class="flash-shell" data-flashcard-quiz>
    <header class="flash-header">
        <div>
            <p class="flash-kicker">@Model.DirectionLabel</p>
            <h1 class="flash-title">@quizName</h1>
            <p class="flash-subtitle">@Text["Quiz.RevealRate"]</p>
        </div>
        @if (Model.SelectedQuiz != null)
        {
            <a asp-controller="Quiz" asp-action="Details" asp-route-id="@Model.SelectedQuiz.Id" class="btn-linkish">@(isFreestyle ? "Back to items" : Text["Quiz.BackWords"])</a>
        }
    </header>

    <div id="flashcardSession" data-flashcard-session>
        @await Html.PartialAsync("_FlashcardSession", Model)
    </div>
</section>

@section Scripts {
<script src="~/js/flashcard-quiz.js" asp-append-version="true"></script>
}
